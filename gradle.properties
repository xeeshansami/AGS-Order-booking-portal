# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
#Default value: -Xmx10248m -XX:MaxPermSize=256m
#org.gradle.jvmargs=-Xmx512m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
android.useAndroidX=true
android.enableJetifier=true
# Keep the pre-AGP-8 transitive R class behaviour so existing resource
# references keep resolving after the AGP 8 upgrade.
android.nonTransitiveRClass=false
# Keep R.id constants final so existing switch/case (case R.id.x) still compiles.
android.nonFinalResIds=false

# ---------------------------------------------------------------------------
# RELEASE SIGNING (do NOT commit real values to version control)
# Fill these in to produce a signed AAB/APK, then run: ./gradlew bundleRelease
# Tip: for safety, put the real values in your GLOBAL ~/.gradle/gradle.properties
# instead of this file so they never get committed.
# ---------------------------------------------------------------------------
# AGS_STORE_FILE=keystore/com.ags.agssalesandroidclientorder.jks
# AGS_STORE_PASSWORD=your_keystore_password
# AGS_KEY_ALIAS=your_key_alias
# AGS_KEY_PASSWORD=your_key_password